# artifact-publisher-cli

A tiny shell-based helper for organizing publication-ready artifacts into a clean export structure.

## Purpose

This repository demonstrates a lightweight workflow utility for preparing documents for distribution, archiving, or public-facing publication.

## Example Tasks

- create export folders
- copy selected files
- standardize output structure
